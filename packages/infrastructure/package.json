{
  "name": "@delulu/infra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm sst dev",
    "sso": "aws sso login --profile=delulu_social",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.840.0",
    "@delulu/typescript-config": "workspace:*",
    "sst": "^3.17.4",
    "typescript": "catalog:"
  }
}
