{
  "extends": "@delulu/typescript-config/nextjs.json",
  "compilerOptions": {
    "baseUrl": ".",
    "composite": false,
    "noEmit": false,
    "paths": {
      "@/*": ["./*"],
      "@delulu/*": ["../../packages/*"]
    },
    "strictNullChecks": true
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "//": "Standalone tsc checks authored handlers and sst-env.d.ts. The SST CLI owns sst.config.ts and its generated .sst vendor implementation; neither belongs in this application's standalone compilation inputs.",
  "exclude": ["node_modules", "dist", ".sst", "sst.config.ts"]
}
