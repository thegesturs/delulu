{
  "extends": "@delulu/typescript-config/nextjs.json",
  "compilerOptions": {
    "baseUrl": ".",
    "composite": false,
    "noEmit": false,
    "paths": {
      "@/*": ["./*"],
      "@delulu/*": ["../../packages/*"]
    },
    "strictNullChecks": true
  },
  "include": ["**/*.ts", "**/*.tsx", ".sst/**/*.ts"],
  "//": "sst.config.ts references ./.sst/platform/config.d.ts for its $config/$app/sst globals, which SST generates at runtime and gitignores. It can only be type-checked by the SST CLI itself, so it is excluded from the standalone tsc pass.",
  "exclude": ["node_modules", "dist", "sst.config.ts"]
}
