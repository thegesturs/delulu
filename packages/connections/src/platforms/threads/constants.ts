/**
 * Threads Graph API version for publish + read (matches the prior provider,
 * which pinned every call to `graph.threads.net/v1.0`).
 */
export const GRAPH_VERSION = "v1.0";

export const PROVIDER = "Threads";
export const CAPTION_LIMIT = 500;
export const MAX_CAROUSEL_IMAGES = 10;
