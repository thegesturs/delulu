{
  "name": "@delulu/payments",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@stripe/agent-toolkit": "^0.6.1",
    "@t3-oss/env-nextjs": "^0.12.0",
    "server-only": "^0.0.1",
    "stripe": "^18.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@delulu/typescript-config": "workspace:*",
    "typescript": "^5.9.2"
  }
}
