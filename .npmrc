# Hoist convex to root node_modules so Docker worker can resolve it
public-hoist-pattern[]=convex*

# Huge Icons Pro authentication
@hugeicons-pro:registry=https://npm.hugeicons.com
//npm.hugeicons.com/:_authToken=${HUGEICONS_TOKEN}
